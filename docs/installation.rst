============
Installation
============

At the command line::

    $ easy_install plugs-post

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-post
    $ pip install plugs-post
